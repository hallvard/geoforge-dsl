model ngu.nadag

import geoforge.lib

layer GeotekniskUnders {
  identity identifikasjon : Identifikasjon
  geometry område: Areal
  borehull*: layer GB {
    identity identifikasjon : Identifikasjon
    opprettetDato: Date
    geometry posisjon: Posisjon
  }
}