# Model for GeoForge

The abstract model, determines the JSON format generated from or synced with langium files.
Should be used by tools, e.g. to generate code from langium files, don't use the langium files directly.
